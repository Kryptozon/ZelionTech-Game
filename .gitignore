.env
.smoketest/
__pycache__/
*.pyc
*.log
pgdata/
redisdata/
frontend/node_modules/
frontend/dist/
# Confidential seed documents — keep out of git. (Included in local Docker builds.)
knowledge/
# Puzzle exports contain answers/scripts — never commit them (DB is source of truth).
puzzles/
daily_hints/
youtube_scripts/
telegram_hints/
